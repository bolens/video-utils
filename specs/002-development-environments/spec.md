# Development environments

Provide locked Python, FFmpeg/ffprobe, GNU, and ShellCheck tools for the native make check test-all gate. Validate actual remuxing, encoding, stream metadata, and publication behavior with disposable fixtures. Keep the catalog, generated output, source retention, no-clobber publication, and read-only MCP authority unchanged.

Provide Docker, Podman, and Apple adapters for a source-free development image. Preserve command arguments and caller ownership. Keep runtime image publication and explicit codec failure reporting intact.
